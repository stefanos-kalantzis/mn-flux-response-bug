micronaut:
  application:
    name: mnFluxResponseBug
cassandra:
  default:
    clusterName: '"myCluster"'
    contactPoint: '"localhost"'
    port: 9042
    maxSchemaAgreementWaitSeconds: 20
    ssl: true
netty:
  default:
    allocator:
      max-order: 3
